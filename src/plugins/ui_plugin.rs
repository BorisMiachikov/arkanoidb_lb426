use bevy::prelude::*;

/// Плагин: UI — счёт, жизни, уровень, активные бонусы
pub struct UiPlugin;

impl Plugin for UiPlugin {
    fn build(&self, _app: &mut App) {
        // TODO (Этап 11): добавить отображение счёта, жизней, уровня
    }
}
